name: CI/CD

on:
  push:

  pull_request:
    branches: [ development ]

jobs:
  build-test:
    name: Base test

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [ 12.x ]

    outputs:
      output1: ${{ steps.installed_dependencies.outputs }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache dependencies
        uses: actions/cache@v2
        with:
          path: |
            **/node_modules
          key: ${{ runner.os }}-dependencies-${{ hashFiles('**/package-lock.json') }}

      - name: Install dependencies
        run: npm install

      - name: Run the tests and generate coverage report
        run: npm run test-coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1
        if: github.ref == 'refs/heads/master'
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Build
        run: npm run build

          #      - name: Set tag version name
          #        id: version-name
          #        if: github.ref == 'refs/heads/pre-production'
          #          || github.ref == 'refs/heads/production'
        #          || github.ref == 'refs/heads/master'
        #        run: echo ::set-output name=short_ref::${GITHUB_REF#refs/*/}

      - name: Get branch name
        id: get_branch
        if: github.ref == 'refs/heads/development'
          || github.ref == 'refs/heads/pre-production'
          || github.ref == 'refs/heads/production'
          || github.ref == 'refs/heads/master'
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"

      - name: Create version and push tag
        uses: mathieudutour/github-tag-action@v5.5
        if: github.ref == 'refs/heads/development'
          || github.ref == 'refs/heads/pre-production'
          || github.ref == 'refs/heads/production'
          || github.ref == 'refs/heads/master'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          tag_prefix: ${{ steps.get_branch.outputs.branch }}
#          release_name: Release ${{ github.ref }}
#          release_branches: development.*
#          pre_release_branches: pre-production

#          - name: Get version and push tag
#            id: tag_version
#            uses: mathieudutour/github-tag-action@v5.5
#            if: github.ref == 'refs/heads/pre-production'
#              || github.ref == 'refs/heads/production'
#              || github.ref == 'refs/heads/master'
#            with:
#              github_token: ${{ secrets.GITHUB_TOKEN }}
#              tag_name: echo ${{ steps.version-name.outputs.short_ref }}-${{ github.ref }}
#              release_name: Release ${{ github.ref }}
#              release_branches: production.*,master
#              pre_release_branches: pre-production

#  - name: Initialize CodeQL
#    uses: github/codeql-action/init@v1
#    with:
#      languages: javascript

#  - name: Perform CodeQL Analysis
#    uses: github/codeql-action/analyze@v1

#  - name: Deploy
#      run: |
#        git config --global user.name $user_name
#        git config --global user.email $user_email
#        git remote set-url origin https://${github_token}@github.com/${repository}
#        npm run deploy
#      env:
#        user_name: 'github-actions[bot]'
#        user_email: 'github-actions[bot]@users.noreply.github.com'
#        github_token: ${{ secrets.ACTIONS_DEPLOY_ACCESS_TOKEN }}
#        repository: ${{ github.repository }}
